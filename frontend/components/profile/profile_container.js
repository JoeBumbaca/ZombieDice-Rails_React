import Profile from './profile';

import { connect } from "react-redux";

const MSP = (state) => {
  return ({

  })
};

const MDP = (dispatch) => {
  return ({

  })
};

export default connect(MSP, MDP)(Profile);