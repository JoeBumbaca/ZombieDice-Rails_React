import { connect } from 'react-redux';
import SetupPage from './setup';

const MSP = (state) => {
  return ({

  })
};

const MDP = (dispatch) => {
  return ({

  })
};

export default connect(MSP, MDP)(SetupPage);