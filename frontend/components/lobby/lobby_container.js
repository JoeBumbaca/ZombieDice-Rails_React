import { connect } from 'react-redux';
import LobbyPage from './lobby';

const MSP = (state) => {
  return ({

  })
};

const MDP = (dispatch) => {
  return ({

  })
};

export default connect(MSP, MDP)(LobbyPage);